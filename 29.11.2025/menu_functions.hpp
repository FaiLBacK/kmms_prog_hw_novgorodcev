#pragma once

#include "menu.hpp"

namespace NovSev {
    const MenuItem* exit(const MenuItem* current);

	const MenuItem* Play_football(const MenuItem* current);
	const MenuItem* Learning_programming(const MenuItem* current);
	
	const MenuItem* Learning_languages_of_programming(const MenuItem* current);
	const MenuItem* Alhoritms_and_structs(const MenuItem* current);
	const MenuItem* Other_technologies(const MenuItem* current);
	const MenuItem* Enter_main_menu(const MenuItem* current);
	
	const MenuItem* Bases_of_data(const MenuItem* current);
	const MenuItem* Multithreaded_programming(const MenuItem* current);
	const MenuItem* Network_technologies(const MenuItem* current);
}
	